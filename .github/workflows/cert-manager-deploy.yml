# This workflow is disabled - Let's Encrypt functionality has been commented out
name: "Disabled - Deploy cert-manager"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow has been disabled - Let's Encrypt functionality has been commented out"
          exit 1
