grafana:
  extraVolumes:
    - name: cpu-alerts
      configMap:
        name: cpu-alerts-grafana-rules
  extraVolumeMounts:
    - name: cpu-alerts
      mountPath: /bitnami/grafana/conf/provisioning/alerting
      readOnly: true
