apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rsschool-flask-app.fullname" . }}
  labels:
    app: {{ include "rsschool-flask-app.name" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "rsschool-flask-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "rsschool-flask-app.name" . }}
    spec:
      containers:
        - name: flask-app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 5000 
