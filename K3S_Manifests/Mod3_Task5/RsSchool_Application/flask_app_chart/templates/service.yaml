apiVersion: v1
kind: Service
metadata:
  name: {{ include "rsschool-flask-app.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
  selector:
    app: {{ include "rsschool-flask-app.name" . }} 
